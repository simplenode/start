

# 启用 ROOT 登录 (互动询问)
```
wget https://raw.githubusercontent.com/simplenode/start/master/server/enable_root_login.sh
sudo chmod +x enable_root_login.sh && sudo ./enable_root_login.sh
```


# 启用 ROOT 登录 (一键修改)
```
# 适合批量 --> 第二行PASSWORD 替换为密码
wget https://raw.githubusercontent.com/simplenode/start/master/server/enable_root_login.sh
sudo chmod +x enable_root_login.sh && sudo ./enable_root_login.sh PASSWORD
```
